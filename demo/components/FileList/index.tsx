import * as React from 'react';

import { Basic } from './Basic';
import { Story } from '../Story';

export const FileList = (): React.ReactElement => (
  <Story title="FileList">
    <Basic title="Basic example" />
  </Story>
);
